{
  "name": "codiceicons",
  "version": "1.0.0",
  "description": "Biblioteca de iconos personalizados para Códice",
  "scripts": {
    "icons": "node scripts/generate-icons.js",
    "icons:min": "cleancss -o icons.min.css icons.css",
    "build": "npm run icons && npm run icons:min"
  },
  "devDependencies": {
    "clean-css-cli": "^5.6.3"
  }
}